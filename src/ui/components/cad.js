import React, {Component} from 'react';
import {observer} from 'mobx-react';

class CadComponent extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (

        );
    };
}


export const Cad = observer((CadComponent));
